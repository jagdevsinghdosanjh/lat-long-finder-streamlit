# Lat‑Long Finder (Streamlit Cloud Version)

Deploy this folder on Streamlit Cloud.

Add secrets in:
    cloud_version/.streamlit/secrets.toml

Logs are stored in:
    access_log_cloud.csv
(They reset on redeploy because Streamlit Cloud storage is ephemeral.)
